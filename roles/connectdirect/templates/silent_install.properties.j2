# Connect:Direct silent install response file
# Adjust keys according to the approved Connect:Direct installer version.

INSTALL_DIR={{ connectdirect_install_dir }}
LOCAL_NODE={{ connectdirect_local_node }}
LISTEN_PORT={{ connectdirect_listen_port }}
RUNTIME_USER={{ connectdirect_runtime_user }}
RUNTIME_GROUP={{ connectdirect_runtime_group }}
